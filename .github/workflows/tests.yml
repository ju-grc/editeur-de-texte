name: tests unitaires

on:
  push:
    branches:
      - *

jobs:
  job1 :
    runs-on: ubuntu-latest
    steps:
      - name : checkout
        uses: actions/checkout@v4
      - name : installer java
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'oracle'
      - name : execute
        uses: mvn test